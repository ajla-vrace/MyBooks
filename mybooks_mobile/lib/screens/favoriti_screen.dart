import 'package:flutter/material.dart';

class FavoritiScreen extends StatelessWidget {
  const FavoritiScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        "FAVORITI",
        style: TextStyle(fontSize: 24),
      ),
    );
  }
}